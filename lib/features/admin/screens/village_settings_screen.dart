import 'package:flutter/material.dart';

class VillageSettingsScreen extends StatelessWidget {
  const VillageSettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: SizedBox.shrink());
  }
}
